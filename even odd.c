#include<stdio.h>
#include<conio.h>
  int main()
  {
     int a,b;
    printf("enter the first number");
    scanf("%d",&a);
    printf("enter the second number");
    scanf("%d",&b);
    if(a%2==0)
    {
      printf("the number %d is even",a);
    }
    else
    {
      printf("the number %d is odd",b);
    }
    getch();
    retrun 0;
  }
